import styled from "styled-components"; 

export const ContainerDatePickerNew = styled.div`
  
  .header-title {
    font-family: Rubik;
    font-style: normal;
    font-weight: 500;
    font-size: 14px;
  }
  .error {
    font-size: 12px;
    font-weight: 500;
    color: var(--color-danger);
    width: max-content;
    height: 15px;
}
`; 